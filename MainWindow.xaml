<Window x:Class="VolumeOSD.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VolumeOSD"
        Title="VolumeOSD" Height="120" Width="250" 
        ResizeMode="NoResize" 
        WindowStartupLocation="Manual"
        Topmost="True" 
        ShowInTaskbar="False" 
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent">
    <Window.Resources>
        <local:StringToColorConverter x:Key="StringToColorConverter"/>
        <Style x:Key="NonTransparentText" TargetType="TextBlock">
            <Setter Property="Opacity" Value="1"/>
        </Style>
        <Style x:Key="CustomProgressBar" TargetType="ProgressBar">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border x:Name="PART_Track" 
                                    Background="Black" 
                                    BorderBrush="Transparent" 
                                    BorderThickness="0" 
                                    CornerRadius="5"/>
                            <Border x:Name="PART_Indicator" 
                                    Background="{Binding ProgressBarColor, Converter={StaticResource StringToColorConverter}}" 
                                    BorderBrush="Transparent" 
                                    BorderThickness="0" 
                                    CornerRadius="5" 
                                    HorizontalAlignment="Left">
                                <Border.Width>
                                    <MultiBinding>
                                        <MultiBinding.Converter>
                                            <local:ProgressToWidthConverter/>
                                        </MultiBinding.Converter>
                                        <Binding Path="Value" RelativeSource="{RelativeSource TemplatedParent}"/>
                                        <Binding Path="ActualWidth" RelativeSource="{RelativeSource TemplatedParent}"/>
                                        <Binding Path="Minimum" RelativeSource="{RelativeSource TemplatedParent}"/>
                                        <Binding Path="Maximum" RelativeSource="{RelativeSource TemplatedParent}"/>
                                    </MultiBinding>
                                </Border.Width>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Window.DataContext>
        <x:Static Member="local:Settings.Current"/>
    </Window.DataContext>
    <Border CornerRadius="10">
        <Border.Background>
            <SolidColorBrush Color="{Binding BackgroundColor, Converter={StaticResource StringToColorConverter}}" Opacity="{Binding BackgroundOpacity}"/>
        </Border.Background>
        <Grid>
            <StackPanel Margin="10" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar x:Name="VolumeProgressBar" 
                           Width="200" Height="30" 
                           Minimum="0" Maximum="100"
                           Style="{StaticResource CustomProgressBar}"/>
                <TextBlock x:Name="VolumePercentText" 
                         Text="0%" 
                         Foreground="{Binding TextColor, Converter={StaticResource StringToColorConverter}}" 
                         FontSize="{Binding FontSize}"
                         Style="{StaticResource NonTransparentText}"
                         HorizontalAlignment="Center" 
                         Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
